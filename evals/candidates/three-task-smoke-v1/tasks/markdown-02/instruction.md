# markdown-02 — Constrained Markdown transformation

Read only `input.txt`. Return Markdown using exactly these level-two headings,
in this order: `Completed` and `Unverified`.

Under each heading, use the fixed bullet wording requested by the input. Do not
add a title, a summary, a third heading, a date, a recommendation, or a claim
that user acceptance occurred.
