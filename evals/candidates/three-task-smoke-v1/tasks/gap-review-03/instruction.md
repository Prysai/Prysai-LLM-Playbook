# gap-review-03 — Evidence-gap review

Read only `input.txt`. Return one JSON object with exactly four fields:
`claim`, `evidence_present`, `evidence_gap`, and `status`.

`evidence_present` and `evidence_gap` must be arrays of strings. Preserve the
build evidence that exists. Do not say that the feature is live, complete, or
verified.
