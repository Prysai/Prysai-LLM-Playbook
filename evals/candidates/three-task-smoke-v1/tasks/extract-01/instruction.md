# extract-01 — Structured extraction

Read only `input.txt`. Return a JSON array with exactly three objects in this
order: `build`, `mobile_390px`, and `user_acceptance`.

Each object must have exactly these string fields: `claim`, `status`, and
`evidence`. Do not add prose, a code fence, dates, owners, test names, or facts
that are not in the input.
